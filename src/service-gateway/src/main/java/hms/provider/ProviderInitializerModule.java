package hms.provider;

/*
import com.google.inject.AbstractModule;

public class ProviderInitializerModule extends AbstractModule {
	@Override
	protected void configure() {		
        bind(hms.provider.repositories.IProviderRepository.class)
        	.to(hms.provider.repositories.ProviderRepository.class);
        bind(hms.provider.IProviderInitializingService.class).to(hms.provider.ProviderInitializer.class);        
	}
}*/
